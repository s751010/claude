# CLAUDE.md — تعليمات دائمة لمشروع CloudMenu

> اقرأ هذا الملف في بداية كل جلسة. المصدر الأصلي للمواصفات هو
> `CLOUDMENU_MASTER_BRIEF.md` — أي تعارض بينه وبين هذا الملف، الـ Brief يفوز.
> ثم اقرأ `PROGRESS.md` لتعرف أين توقّفنا.

---

## 1. ما هو المشروع

منصة منيوهات رقمية للمطاعم في السوق السعودي. المطعم يشترك شهرياً، ويبني منيو،
ويحصل على QR يفتحه زبائنه على جوالاتهم.

**نموذج الإيراد:** اشتراك شهري/سنوي من المطعم إلى CloudMenu مباشرة. لا شيء غير هذا.

---

## 2. القوانين الحديدية

خرقها = فشل المهمة. ليست تفضيلات.

### 2.1 نظافة الملفات

**ممنوع منعاً مطلقاً** أي ملف يحتوي على:

```
_old  _new  _backup  _copy  _final  _v2  _v3  -2  (1)
.bak  .orig  temp_  tmp_  test_  _draft  _fix
```

- تعديل ملف؟ **عدّله في مكانه.** git هو النسخ الاحتياطي.
- ملف صار غير مستخدم؟ **احذفه.** لا تتركه "احتياطاً".
- تجربة نهج بديل؟ فرع git، لا ملف موازٍ.

### 2.2 مصدر واحد للحقيقة

كل ثابت يُعرَّف في **مكان واحد** ويُستورد منه:

| الملف | المحتوى |
|---|---|
| `src/config/pricing.ts` | الأسعار ورسوم بيلينك. **القيَم الرقمية المالية الوحيدة في المشروع** |
| `src/config/plans.ts` | حدود كل باقة |
| `src/config/themes.ts` | الثيمات التسعة |
| `src/config/strings.ts` | كل نص عربي |
| `src/config/constants.ts` | أي ثابت آخر |

**قاعدة:** رقم سعر أو حد باقة مكتوب حرفياً في أي مكوّن أو صفحة → **خطأ يُصلَح فوراً.**

> هذه المشكلة كانت حقيقية في المشروع القديم: مصفوفة الأسعار مكرّرة حرفياً في مكوّنين،
> وعدد الثيمات مكتوب بثلاث قيم متناقضة (5 و 12 و 8). انظر `AUDIT.md` — B3 و B4.

### 2.3 ⚠️ قاعدة الحقول الثلاثة

أخطر بق في المشروع القديم. أي حقل جديد يجب أن يظهر في ثلاثة أماكن في اللحظة نفسها:
تهيئة النموذج · قائمة السماح للإضافة · قائمة السماح للتحديث.
نسيان واحدة = **الحقل يُحذف بصمت** ولا يظهر خطأ.

**نمنعها معمارياً — لا بالانضباط.** لكل كيان schema واحد بـ zod، والثلاثة تُشتقّ منه:

```ts
export const dishSchema       = z.object({ /* كل الحقول مرة واحدة، هنا فقط */ });
export const dishInsertSchema = dishSchema.omit({ id: true, created_at: true });
export const dishUpdateSchema = dishInsertSchema.partial();
export const dishDefaults     = dishInsertSchema.parse({ /* … */ });
```

**كل** جسم يمر عبر `schema.parse()` في `src/lib/db/` قبل إرساله. الحقل المجهول يُرفض
بخطأ صريح بدل أن يُحذف بصمت.

**عند إضافة حقل:** سطر واحد في الـ schema + migration. لا شيء آخر.

### 2.4 التوثيق الحي

| الملف | متى يُحدَّث |
|---|---|
| `CLAUDE.md` | عند أي تغيير معماري |
| `PROGRESS.md` | نهاية كل مرحلة |
| `AUDIT.md` | لا يُحدَّث — وثيقة تاريخية للمشروع القديم |
| `PLAN.md` | عند تغيير الخطة بموافقة صاحب المشروع |

`PROGRESS.md` هو الذاكرة بين الجلسات. يُكتب لشخص لا يعرف شيئاً عن المشروع.

---

## 3. المعمارية والحزمة التقنية

### مسموح

| الطبقة | الاختيار |
|---|---|
| الإطار | Next.js (أحدث مستقر) — App Router |
| اللغة | TypeScript، `strict: true` |
| التنسيق | Tailwind CSS |
| الخلفية | Supabase (Postgres + Auth + Storage + Edge Functions) |
| التحقق | Zod |
| النماذج | React Hook Form |
| الحالة | حالة React الأصلية + Server Components. **لا مكتبة حالة عامة** |
| الأيقونات | lucide-react |
| النشر | Netlify |

### ممنوع دون موافقة صريحة

أي ORM (Prisma/Drizzle) · أي مكتبة UI ثقيلة · أي مكتبة حالة عامة (redux/zustand) ·
أي حزمة رسوم بيانية قبل الحاجة الفعلية.

> البندل القديم كان يحمل `recharts` و `redux` بلا داعٍ — مساهمان رئيسيان في حجم 1.4MB.

### قواعد الكود

- **حد أقصى 300 سطر للملف.** أطول → قسّمه.
- المكوّن يفعل شيئاً واحداً.
- منطق الأعمال في `src/lib/`، لا في المكوّنات.
- **كل استدعاء Supabase عبر دوال في `src/lib/db/`** — لا استدعاءات متفرقة في الصفحات.
- **كل نص عربي في `src/config/strings.ts`** — لا نصوص حرفية في JSX.
- **كل استدعاء يمكن أن يفشل يجب أن يعالج الفشل. لا `catch` فارغ.**

---

## 4. خريطة الوكلاء

المشروع منظّم بأسماء عربية. صاحب المشروع يقول «عدّل عند نورة» ويقصد محرر المنيو.

**قاعدتك:** عند ذكر اسم → عدّل **ذلك المجال فقط**، وأبلغ صراحةً عن أي تبعية على مجال آخر
**قبل** التعديل.

### لوحة المطعم

| الاسم | المجال | المسار |
|---|---|---|
| **راشد** | المنيو العام: الثيمات، بطاقات الأطباق، السلة، الخيارات | `src/app/m/[slug]/` |
| **خالد** | الدفع: بيلينك، الاشتراكات، الفواتير | `src/app/dashboard/billing/` |
| **نورة** | محرر المنيو: الأطباق، الأقسام، الخيارات | `src/app/dashboard/menu-editor/` |
| **سلمى** | التقييمات: تقييمات الزبائن، الاستبيان | `src/app/dashboard/reviews/` |
| **عمر** | الولاء: نظام الأختام | `src/app/dashboard/loyalty/` |
| **ماجد** | الذكاء الاصطناعي: استيراد المنيو، توليد الأوصاف | `src/app/dashboard/ai/` |
| **ريم** | الدعم: التذاكر | `src/app/dashboard/support/` |
| **سعد** | الإعدادات العامة: المتجر، ساعات العمل، الحساسية، Google | `src/app/dashboard/settings/` |

### لوحة المؤسس

| الاسم | المجال | المسار |
|---|---|---|
| **فهد** | النظرة العامة: صحة النظام، سجل النشاط، مفاتيح الميزات | `src/app/founder/` |
| **لمياء** | المطاعم: إدارة المطاعم والمستخدمين | `src/app/founder/restaurants/` |
| **طارق** | الإيرادات: أكواد الخصم، الاشتراكات المنتهية، **التفعيل اليدوي** | `src/app/founder/revenue/` |
| **هند** | المحتوى: الإعلانات، المدونة، الفوتر | `src/app/founder/content/` |
| **يوسف** | الفريق: الفريق والصلاحيات | `src/app/founder/team/` |

**اذكر الاسم العربي في تعليق أعلى كل ملف رئيسي:**

```ts
// نورة — محرر المنيو
```

> **تنبيه:** المشروع القديم كان فيه ميزة "فريق استشاري AI" بشخصيات تحمل نفس الأسماء
> بأدوار مختلفة (نورة = تسويق، خالد = نجاح عملاء…). **الميزة محذوفة** وهذه الأسماء تعني
> خريطة الوكلاء فقط، لا شيء غيرها.

---

## 5. ⚠️ القيد القانوني الحرج — الدفع

شروط بيلينك تنص على أن الأنشطة العاملة بنموذج **الحسابات التجميعية** — وتشمل صراحةً
منصات التجارة الإلكترونية متعددة التجار — تحتاج ترخيصاً من البنك المركزي السعودي (ساما)
أو خطاب عدم ممانعة.

✅ **مسموح — نموذجنا:**
> CloudMenu تحصّل اشتراكاً شهرياً من المطعم مقابل الخدمة. المال من المطعم إلى CloudMenu
> مباشرة. لا تجميع.

🚫 **ممنوع — لا تبنه ولو طُلب منك:**
> زبون المطعم يدفع قيمة طلبه داخل المنيو، وCloudMenu تستلم المال ثم تحوّله للمطعم.
> نشاط تجميعي يحتاج ترخيص ساما.

**لذلك: المنيو العام لا يحتوي بوابة دفع.** السلة تنتهي برسالة واتساب للمطعم أو استدعاء
طلب. لا شيء آخر.

> المشروع القديم كان فيه بالضبط هذا الممنوع: مفتاح ميزة `payment_enabled` بوصف
> «بوابة دفع أونلاين داخل المنيو». **محذوف بالكامل.** انظر `AUDIT.md` — C3.

### قواعد أمنية غير قابلة للتفاوض

1. `apiId` و `secretKey` **على السيرفر فقط**. أي مفتاح في كود العميل = فشل فوري.
2. كل استدعاءات بيلينك عبر Edge Function أو Route Handler. الواجهة لا تلمس بيلينك.
3. **Webhook هو الحقيقة، لا `callBackUrl`.** المستخدم يفتح رابط النجاح يدوياً.
   **لا تفعّل اشتراكاً إلا من webhook مُتحقَّق منه.**
4. تحقق من صحة الـ webhook قبل معالجته.
5. اجعل المعالجة **idempotent** — بيلينك قد ترسل نفس الحدث مرتين.
6. سجّل كل حدث في `payment_events` **قبل** معالجته.
7. `.env.local` في `.gitignore`. `.env.example` بأسماء المتغيرات بلا قيَم.

### الأسعار

| الباقة | شهري | سنوي |
|---|---|---|
| **نمو** | 99 ر | 1129 ر |
| **احتراف** | 199 ر | 2269 ر |

باقتان فقط. **لا توجد باقة "أساسي"** — حُذفت بقرار صاحب المشروع.

**رسوم بيلينك (باقة البدء)** — تُوضع في `src/config/pricing.ts` ولا تُكتب حرفياً في أي مكان:
مدى 1% · فيزا/ماستر 2.75% · ثابت 1 ريال لكل عملية · تحويل بنكي 5 ريال ·
بلا اشتراك شهري · بلا رسوم تأسيس.

جدول `payments` يخزّن: الإجمالي، الرسوم، **الصافي**. لوحة الإيرادات (طارق) تعرض
**الصافي** لا الإجمالي.

---

## 6. أمان قاعدة البيانات

**RLS على كل جدول بلا استثناء.** ليست "مهمة لاحقة".

الدروس المستخلصة من تدقيق المشروع القديم — هذه أخطاء **حدثت فعلاً** ولا تتكرر:

| القاعدة | لماذا |
|---|---|
| `subscriptions`: لا `INSERT`/`UPDATE` من أي دور عميل | القديم سمح للمستخدم بمنح نفسه أعلى باقة — `AUDIT.md` C2 |
| صلاحية المؤسس من عمود دور في قاعدة البيانات، لا من مقارنة إيميل في العميل | القديم فتح اللوحة لأي شخص يكتب إيميلاً معيناً — C1 |
| لا بروكسي عام يستقبل اسم جدول من العميل | القديم كان فيه `founder-admin` كذلك — C5 |
| `loyalty_customers`: لا وصول من دور anon | القديم كشف أرقام جوالات الزبائن — C7 |
| `analytics`: الزيادة عبر دالة `security definer` فقط | القديم كان `upsert` يصفّر العدّاد إلى 1 — B1 |
| حدود الباقات تُفرض في trigger، لا في الواجهة وحدها | القديم عرّف الحدود ولم يطبّقها أبداً — B2 |

---

## 7. المنيو العام (راشد) — أهم صفحة

يفتحها زبون من QR على جواله، غالباً على شبكة بطيئة. الأولويات بالترتيب:

1. **السرعة** — Server Components، صور محسّنة، أقل JS ممكن
2. **الموبايل أولاً** — صمّم للجوال ثم وسّع
3. **RTL أصلي** — تصميم عربي، لا انعكاس واجهة إنجليزية
4. **الثيمات** — تسعة، كلها تتشارك البنية وتختلف في الرموز فقط

**التصميم:** اقرأ توجيهات `frontend-design` قبل البدء.

- الثيمات **كرموز، لا كأوراق أنماط منفصلة**. كل ثيم = مجموعة متغيرات CSS.
- تجنّب المظهر الافتراضي المعروف لواجهات الذكاء الاصطناعي: خلفية كريمية + خط serif
  عالي التباين + لون طيني دافئ. لكل ثيم عالَم بصري خاص من مطبخه.
- الخطوط: لكل ثيم **زوج عربي مقصود** (عرض + متن).
- الحركة: بأقل قدر، وباحترام `prefers-reduced-motion`.

**الحد الأدنى للجودة:** استجابة كاملة للموبايل · تركيز لوحة مفاتيح مرئي · تباين لوني كافٍ ·
حالات فارغة تدعو للفعل · رسائل خطأ تشرح ما حدث وكيف يُحل.

---

## 8. الأوامر

```bash
npm run dev      # التطوير
npm run build    # يجب أن ينجح بصفر أخطاء TypeScript
npm run lint     # يجب أن ينجح بصفر تحذيرات
npm run start    # تشغيل النسخة المبنية
```

**قبل أي commit:** `npm run build && npm run lint` — كلاهما ينجح، أو لا commit.

---

## 9. معايير القبول

لا تنتهي أي مرحلة حتى:

- [ ] `npm run build` ينجح — صفر أخطاء TypeScript
- [ ] `npm run lint` ينجح — صفر تحذيرات
- [ ] لا ملف يخرق قواعد التسمية في القسم 2.1
- [ ] لا رقم سعر أو حد باقة مكتوب حرفياً خارج `src/config/`
- [ ] كل جدول جديد له سياسة RLS
- [ ] كل حقل جديد يمر عبر schema واحد
- [ ] لا مفتاح أو سر في كود العميل
- [ ] `PROGRESS.md` محدّث
- [ ] كل صفحة جديدة تعمل على عرض 375px

---

## 10. كيف تتواصل

- **بالعربي.** المصطلحات التقنية بالإنجليزي عادي.
- اشرح بجداول ونقاط، لا بفقرات طويلة.
- عند اتخاذ قرار معماري: اذكره صريحاً مع السبب. **لا تقرر بصمت.**
- عند مواجهة غموض: **اسأل.** لا تفترض.
- عند العثور على مشكلة: قل إنك وجدتها **فوراً**، لا في النهاية.
- **لا تقل "تم" إلا إذا اختبرته فعلاً.** إن لم تختبره، قل **"مبني، غير مختبر"**.

### بوابات التوقف

| البوابة | متى | ماذا تعرض |
|---|---|---|
| 🛑 **GATE 1** | نهاية المرحلة 0 | `AUDIT.md` + `PLAN.md` |
| 🛑 **GATE 2** | نهاية المرحلة 2 | ملفات SQL + شرح RLS بالعربي. **صاحب المشروع ينفّذها بنفسه** |
| 🛑 **GATE 3** | قبل المرحلة 5 | مخطط تدفق الدفع كاملاً |

**لا تنشئ مشروع Supabase ولا تنفّذ SQL بنفسك.** اعرض الملفات وانتظر.

### صلاحياتك

**لك صلاحية كاملة في:** حذف أي ملف أو ميزة تراها غير مناسبة · إعادة التسمية والهيكلة ·
رفض أي طلب خاطئ تقنياً — بشرط شرح السبب واقتراح البديل.

**ليس لك صلاحية في:** إضافة ميزة غير مذكورة دون سؤال · تخطي بوابات التوقف ·
استخدام مكتبة خارجية غير مذكورة في القسم 3 دون موافقة.
