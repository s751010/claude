# PROGRESS.md — سجل التقدّم

> الذاكرة بين الجلسات. مكتوب لشخص لا يعرف شيئاً عن المشروع.
> آخر تحديث: **2026-07-27**

---

## أين نحن الآن

| البند | الحالة |
|---|---|
| **المرحلة الحالية** | المرحلة 2 — قاعدة البيانات ✅ **مكتملة ومختبَرة** |
| **البوابة** | 🛑 **STOP GATE 2 — بانتظارك** |
| **المطلوب منك** | إنشاء مشروع Supabase، تنفيذ ملفات SQL الأربعة بالترتيب، ترقية حسابك إلى `founder`، ثم إعطائي مفاتيح البيئة |
| **الفرع** | `claude/new-session-odtmpz` |
| **الخطوة التالية** | المرحلة 3: لوحة المطعم (سعد ← نورة ← عمر ← سلمى ← ريم ← ماجد) |

---

## ما أُنجز

### المرحلة 0 — التحليل ✅

| المخرَج | الوصف |
|---|---|
| `AUDIT.md` | تدقيق كامل للمشروع القديم: 7 ثغرات حرجة، 9 بق مؤكّد، 8 مكوّنات قابلة لإعادة الاستخدام، 9 عناصر مقترحة للحذف |
| `PLAN.md` | شجرة الملفات النهائية، الحل المعماري لقاعدة الحقول الثلاثة، الثيمات التسعة، ترتيب البناء، البوابات |
| `CLAUDE.md` | التعليمات الدائمة: القوانين، المعمارية، خريطة الوكلاء، القيد القانوني، الأوامر |
| `PROGRESS.md` | هذا الملف |

**ما فُحص:** ملف `index_19.html` المرفوع — بندل React مُصغّر، 1,486,607 بايت، 1947 سطر،
منشور على `cloudsmenu.netlify.app`، متصل بمشروع Supabase `wjqpsbpebpntpeinqccl`.

### المرحلة 1 — الأساس ✅

**الحزمة المثبَّتة:** Next.js 16.2.12 (App Router) · React 19.2.4 · TypeScript 5 ·
Tailwind CSS v4 · zod 4.4.3 · ESLint 9 · Prettier.

**ما بُني:**

| الملف | المحتوى |
|---|---|
| `src/config/pricing.ts` | الباقتان + رسوم بيلينك + `calculateFees()` لحساب الصافي. **الملف الوحيد الحاوي قيَماً مالية** |
| `src/config/plans.ts` | حدود الباقتين + `canAddMenu()` و `canAddDish()`. `null` = غير محدود (بدل الرقم السحري 999) |
| `src/config/themes.ts` | عقد الرموز + الثيمات التسعة (بنية فقط) + `themeToCssVars()`. `THEME_COUNT` **مشتقّ لا مكتوب** — يمنع تكرار B3 |
| `src/config/strings.ts` | كل النصوص العربية. رسائل الخطأ تشرح ما حدث وكيف يُحل |
| `src/config/constants.ts` | حدود الحقول، قيود الصور، نمط الـ slug، الكلمات المحجوزة |
| `src/schemas/common.ts` | لبنات مشتركة + تطبيع رقم الجوال السعودي (منقول من القديم) |
| `src/schemas/{dish,menu,restaurant,subscription}.ts` | schema واحد لكل كيان، والثلاثة تُشتقّ منه |
| `src/app/layout.tsx` | `dir="rtl"` و `lang="ar-SA"` على `<html>` + خط IBM Plex Sans Arabic |
| `src/app/globals.css` | متغيرات اللوحة + تركيز لوحة مفاتيح مرئي + احترام `prefers-reduced-motion` |
| `.env.example` | أسماء المتغيرات بلا قيَم، مع تحذير `NEXT_PUBLIC_` |

**نتائج الاختبار الفعلي** (لا ادعاء — شُغِّل وفُحص):

| الفحص | النتيجة |
|---|---|
| `npm run build` | ✅ نجح — صفر أخطاء TypeScript |
| `npm run lint` | ✅ نجح — صفر تحذيرات |
| `npm run typecheck` | ✅ نجح |
| الصفحة على `localhost:3000` | ✅ HTTP 200 |
| `<html dir lang>` | ✅ `dir="rtl"` · `lang="ar-SA"` |
| الخط العربي | ✅ IBM Plex Sans Arabic مُضمَّن ذاتياً بـ `unicode-range: U+6??` (16 ملف woff2) |
| عرض 375px | ✅ `scrollWidth === clientWidth === 375` — لا تجاوز أفقي (لقطة بـ Chromium) |
| الأسعار والحدود | ✅ تُقرأ من `config/` — 99/1129 و 199/2269 و «غير محدود» |

**فحوص القبول الآلية:** لا ملف يخرق قواعد التسمية 2.1 · لا رقم سعر خارج
`config/pricing.ts` · لا سرّ في `src/` · كل الملفات تحت 300 سطر (الأطول 205).

**قرار إضافي:** شُدِّد `tsconfig.json` فوق `strict` بـ `noUncheckedIndexedAccess` و
`noImplicitOverride` و `noFallthroughCasesInSwitch` و `allowJs: false` — لأن بقّ المشروع
القديم كان من نوع «الفشل الصامت»، وهذه الأعلام تكشفه وقت الترجمة.

**ملاحظة:** `npm audit` يبلّغ عن 12 ثغرة في سلسلة أدوات ESLint (`brace-expansion`).
كلها **تبعيات تطوير فقط**، ولا شيء منها في حزمة الإنتاج. إصلاحها يتطلب ترقية كاسرة
لـ ESLint 10 — أُجّل حتى تصدر `eslint-config-next` متوافقة.

### المرحلة 2 — قاعدة البيانات ✅

أربعة ملفات SQL مرقّمة في `supabase/migrations/`، **مختبَرة فعلياً** على Postgres 16
محلي مؤقت مع محاكاة بيئة Supabase (`auth.users` · `auth.uid()` · `storage.objects` ·
الأدوار `anon`/`authenticated`/`service_role`).

| الملف | المحتوى |
|---|---|
| `001_schema.sql` | 9 أنواع مُعدَّدة · 18 جدولاً · RLS مفعّلة على كلٍّ منها · دوال `increment_menu_view` و `is_founder` و `current_plan` · triggers فرض حدود الباقات |
| `002_rls.sql` | 4 دوال مساعدة + سياسات كل جدول، كل سياسة مشروحة بالعربي ومربوطة برقم الثغرة التي تغلقها · فحص ختامي يوقف التنفيذ إن بقي جدول بلا RLS |
| `003_storage.sql` | bucket `menu-images` — قراءة عامة، وكتابة داخل مجلد المالك فقط |
| `004_seed.sql` | حدود الباقتين + مفاتيح الميزات + محتوى الفوتر (قابل لإعادة التنفيذ) |

**🐛 بقّان حقيقيان اكتشفهما الاختبار — كانا سيصلان إليك لولاه:**

| # | البق | الأثر لو لم يُكتشف |
|---|---|---|
| 1 | **تكرار لا نهائي في السياسات** — سياسة `restaurants` تستعلم عن `menus` وسياسة `menus` تستعلم عن `restaurants` | `ERROR: infinite recursion detected in policy` عند **أي** عملية على المنيوهات أو المطاعم. التطبيق معطّل بالكامل |
| 2 | **حارس الدور يمنع الخطوة اليدوية نفسها** | ترقية أول مؤسس مستحيلة — لوحة المؤسس لا تُفتح أبداً |

**الإصلاح:** كل إشارة من سياسة إلى جدول آخر محمي بـ RLS تمرّ الآن عبر دالة
`security definer` (`owns_restaurant` · `owns_menu` · `restaurant_has_public_menu` ·
`menu_is_public`) — أسرع أيضاً لأنها تتجنّب تقييم سياسات متداخلة لكل صف.
وحارس الدور يستثني السياق الإداري (`auth.uid() is null`).

**نتائج اختبار الاختراق** — بعد منح صلاحيات Supabase الافتراضية لدورَي `anon` و
`authenticated`، بحيث تكون **RLS وحدها هي الحاجز**:

| الاختبار | الثغرة | النتيجة |
|---|---|---|
| مستخدم يمنح نفسه باقة `pro` | C2 | ✅ `violates row-level security policy` |
| زائر يقرأ أرقام جوالات الزبائن | C7 | ✅ صفر صفوف |
| زائر يعدّل أختام زبون | C7 | ✅ `UPDATE 0` |
| مستخدم يرقّي نفسه إلى مؤسس | C1 | ✅ `ROLE_CHANGE_FORBIDDEN` |
| مستخدم عادي يعدّل مفاتيح الميزات | C5 | ✅ `UPDATE 0` |
| إدراج مباشر في `analytics` | B1 | ✅ مرفوض بـ RLS |
| ثلاث مشاهدات متتالية | B1 | ✅ العدّاد = **3** (يتراكم، لا يُصفَّر) |
| منيو رابع على باقة `growth` | B2 | ✅ `PLAN_LIMIT_MENUS` |
| مهاجم يعدّل سعر طبق غيره | عزل | ✅ السعر لم يتغيّر |
| مهاجم يحذف منيو غيره | عزل | ✅ `DELETE 0` |

**والمسار الشرعي سليم:** المالك ينشئ مطعماً ومنيوهات وأطباقاً · الزائر يرى المطاعم
والمنيوهات المنشورة والأطباق ولا يرى الاشتراكات ولا المدفوعات · المؤسس يرى كل شيء
**عدا** أرقام جوالات الزبائن (لا حاجة تشغيلية تبرّرها).

**قرار معماري مُصرَّح به:** المؤسس لا يقرأ `loyalty_customers`، و `payment_events`
مغلق أمام كل دور عميل بلا استثناء.

---

## ⚠️ مفاجأة في التحليل — حالة المستودع

الـ Brief (القسم 1) وصف مشروعاً موزّعاً على مسارين: HTML قديم + Next.js على فرع
`claude/awesome-*` + `main` شبه فارغ.

**الواقع:** مستودع `s751010/claude` **فارغ تماماً** — صفر commits، صفر فروع على `origin`.
لا يوجد فرع `claude/awesome-*`، ولا `main`.

**الأثر على الخطة:** إيجابي. لا هجرة من git، لا فروع متعارضة، لا ملفات مكرّرة نحذفها.
المشكلة الموصوفة في القسم 1 حُلّت بالأمر الواقع. نبدأ من شجرة نظيفة.

**مصدر التدقيق الوحيد** هو البندل المرفوع.

---

## القرارات المتخذة وأسبابها

| # | القرار | السبب |
|---|---|---|
| 1 | **نطاق هذه الجولة: وثائق المرحلة 0 فقط** | التزام حرفي بـ STOP GATE 1 في القسم 3 من الـ Brief |
| 2 | **مشروع Supabase جديد نظيف** | المشروع القديم فيه ثغرات C1/C2/C7 وسياسات RLS مجهولة لا يمكن فحصها من البندل. البدء من صفر أنظف من وراثة ارتخاء غير مرئي |
| 3 | **باقتان فقط: نمو 99 (1129 سنوي) · احتراف 199 (2269 سنوي)** | قرار صاحب المشروع. **حُذفت باقة "أساسي" 49/559** |
| 4 | **9 ثيمات تُصمَّم من الصفر** | القديم فيه 8 خرائط رموز، لكن بعضها قريب من المظهر الافتراضي الذي يحذّر منه القسم 7. البنية تُنقل، القيَم تُصمَّم |
| 5 | **حذف ميزة "فريق الاستشارة بالـ AI"** | خارج نطاق المنتج، **وأسماء شخصياتها تتضارب مع خريطة الوكلاء** (نورة، خالد، ريم، سلمى بأدوار مختلفة) فتربك التواصل |
| 6 | **حذف كل مسار الدفع داخل المنيو** | القسم 6.1 — نشاط تجميعي يحتاج ترخيص ساما. غير قابل للنقاش |
| 7 | **حذف `recharts` و `redux`** | القسم 5 يمنع مكتبة حالة عامة وحزمة رسوم قبل الحاجة. مساهمان رئيسيان في حجم 1.4MB |
| 8 | **قاعدة الحقول الثلاثة تُحلّ بـ zod، لا بالانضباط** | schema واحد لكل كيان تُشتقّ منه التهيئة والإدراج والتحديث. لا يمكن نسيان قائمة إذا لم تكن هناك قوائم |
| 9 | **حدود الباقات تُفرض في طبقتين** | `lib/db/` لرسالة عربية واضحة، و trigger في قاعدة البيانات لأن الواجهة وحدها ليست حاجزاً |

---

## 🚨 مسائل أمنية عاجلة — تخص صاحب المشروع لا الكود

الموقع القديم `cloudsmenu.netlify.app` **منشور الآن** وفيه ثغرتان مستغَلّتان من أي زائر:

| # | الإجراء المطلوب | يعالج |
|---|---|---|
| 1 | إنزال الموقع القديم أو وضع Basic Auth عليه | C1 + C2 |
| 2 | إبطال مفتاح ميسر المنشور وإعادة توليده | C6 |
| 3 | تغيير كلمة مرور حساب المؤسس وإبطال `x-founder-secret` | C1 + C5 |
| 4 | مراجعة جدول `subscriptions` القديم بحثاً عن اشتراكات ممنوحة ذاتياً | C2 |
| 5 | مراجعة RLS على `loyalty_customers` (أرقام جوالات زبائن) | C7 |

**الحالة:** لم يُتخذ أي منها بعد — كلها خارج صلاحية الكود وتحتاج وصول صاحب المشروع.

**لم يُنقل أي مفتاح أو سر من المشروع القديم، ولن يُنقل.**

---

## ⚠️ التكرار المعروف — تنازل مقصود ومُعلَن

حدود الباقات موجودة في **مكانين**:

| المكان | الغرض |
|---|---|
| `src/config/plans.ts` | الواجهة: عرض ما تشمله كل باقة، ومنع الزر قبل رحلة الشبكة |
| جدول `plan_limits` (يُملأ في `004_seed.sql`) | الفرض في trigger داخل قاعدة البيانات |

**السبب:** الـ trigger لا يقرأ TypeScript، والواجهة لا يجوز أن تنتظر رحلة شبكة لتعرف
حدّ الباقة. الفرض في الطبقتين مقصود — الواجهة لرسالة عربية واضحة، وقاعدة البيانات
كحاجز أمني حقيقي (AUDIT.md — B2).

**القاعدة الملزِمة:** تغيير أي رقم في أحدهما يستوجب تغييره في الآخر **في نفس الـ commit**.

**الحل المقترح للمرحلة 7:** توليد `004_seed.sql` من `plans.ts` بسكربت، فيعود المصدر واحداً.
لم يُبنَ الآن لأنه سيعقّد المرحلة 2 بلا داعٍ.

---

## البقّ المعروف في المشروع القديم — وأين تُعالَج

| # | البق | يُعالَج في |
|---|---|---|
| B1 | عدّاد المشاهدات لا يتراكم (`upsert` يصفّره إلى 1) | المرحلة 2 — دالة `increment_menu_view()` |
| B2 | حدود الباقات معرَّفة ولا تُطبَّق إطلاقاً | المرحلة 2 (trigger) + 3 (`lib/db/`) |
| B3 | عدد الثيمات بثلاث قيم متناقضة (5 · 12 · 8) | المرحلة 1 — `src/config/themes.ts` |
| B4 | مصفوفة الأسعار مكرّرة في مكوّنين | المرحلة 1 — `src/config/pricing.ts` |
| B5 | الخصم يُحسب في العميل ويُكتب في سجل الإيراد | المرحلة 2 (RLS) + 5 |
| B6 | ثيم الـ AI مخزَّن كـ JSON في عمود نصّي | المرحلة 2 — `theme_id` + `custom_theme jsonb` |
| B7 | جلسة JWT يدوية في `localStorage` | المرحلة 1 — `@supabase/ssr` |
| B8 | `promo_codes` بمصدرين (جدول + localStorage) | المرحلة 6 |
| B9 | `catch` فارغ في مسارات الشبكة | كل المراحل — قاعدة في `CLAUDE.md` |

التفاصيل الكاملة في `AUDIT.md`.

---

## ما تبقّى

| المرحلة | الحالة |
|---|---|
| 0 — التحليل | ✅ مكتملة |
| 1 — الأساس | ✅ مكتملة ومختبَرة |
| 2 — قاعدة البيانات | ✅ مكتملة ومختبَرة — 🛑 **عند GATE 2 الآن** |
| 3 — لوحة المطعم | ⬜ الترتيب: سعد ← نورة ← عمر ← سلمى ← ريم ← ماجد |
| 4 — المنيو العام (راشد) | ⬜ |
| 5 — الدفع (بيلينك) | ⬜ يسبقها 🛑 GATE 3 |
| 6 — لوحة المؤسس | ⬜ يشمل **التفعيل اليدوي** — طريق الإيراد الأول |
| 7 — التشديد والنشر | ⬜ |

---

## أسئلة مفتوحة لصاحب المشروع

| # | السؤال | متى نحتاج الجواب |
|---|---|---|
| 1 | ~~الموافقة على `AUDIT.md` و `PLAN.md`~~ | ✅ تمت — GATE 1 |
| 2 | مفاتيح بيئة Supabase بعد إنشاء المشروع الجديد | نهاية المرحلة 2 |
| 3 | **تأكيد حدود الباقتين** — نُقلت حدود القديم مبدئياً (نمو: 3 منيوهات/100 طبق · احتراف: غير محدود) | **معلّق** — تغييرها سطر واحد في `src/config/plans.ts` |
| 3ب | **ضريبة القيمة المضافة 15%** — الـ Brief لا يذكرها. هل الأسعار شاملة الضريبة أم تُضاف؟ | قبل المرحلة 5 (الدفع) |
| 4 | الموافقة على مفاهيم الثيمات التسعة | بداية المرحلة 4 |
| 5 | أي من التطويرات المقترحة (6–9) نبني؟ | بعد المرحلة 4 |
| 6 | بيانات اعتماد بيلينك (البيئة التجريبية أولاً) | بداية المرحلة 5 |

---

## البيئة

Node v22.22.2 · npm 10.9.7 · git 2.43.0 · مساحة متاحة 30 GB · وصول npm registry يعمل.
