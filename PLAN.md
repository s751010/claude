# PLAN.md — خطة إعادة بناء CloudMenu

> **المرحلة 0 من `CLOUDMENU_MASTER_BRIEF.md`** — الخطة المقترحة.
> تُقرأ بعد `AUDIT.md`. تاريخ: 2026-07-27.

---

## 1. القرارات المحسومة

| القرار | ما اتُّفق عليه | الأثر |
|---|---|---|
| **المستودع** | البدء من شجرة نظيفة على `main` | المستودع فارغ أصلاً — لا هجرة، لا تعارض |
| **Supabase** | **مشروع جديد نظيف** | لا إرث من `wjqpsbpebpntpeinqccl` ولا سياسات RLS مجهولة |
| **الباقات** | **باقتان فقط** — نمو 99 ر/شهر (1129 سنوي) · احتراف 199 ر/شهر (2269 سنوي) | **تُحذف باقة "أساسي" 49/559** |
| **الثيمات** | **9 ثيمات تُصمَّم من الصفر** | بنية الرموز القديمة تُستخدم كهيكل فقط، لا كقيَم |
| **الدفع** | بيلينك فقط، للاشتراكات فقط | لا دفع داخل المنيو — القسم 6.1 |

---

## 2. شجرة الملفات النهائية

```
/
├── CLAUDE.md · PROGRESS.md · AUDIT.md · PLAN.md
├── .env.example · .gitignore · .prettierrc
├── package.json · tsconfig.json · next.config.ts
├── tailwind.config.ts · eslint.config.mjs
│
├── supabase/
│   ├── migrations/
│   │   ├── 001_schema.sql
│   │   ├── 002_rls.sql
│   │   ├── 003_storage.sql
│   │   └── 004_seed.sql
│   └── functions/
│       └── ai-proxy/          ← منقول ومُراجَع
│
└── src/
    ├── config/                ← مصدر الحقيقة الوحيد. لا رقم سعر أو حد خطة خارجه
    │   ├── pricing.ts         ← الباقتان + رسوم بيلينك (القسم 6.4)
    │   ├── plans.ts           ← حدود كل باقة
    │   ├── themes.ts          ← الثيمات التسعة كرموز
    │   ├── strings.ts         ← كل نص عربي
    │   └── constants.ts
    │
    ├── schemas/               ← zod لكل كيان — حلّ قاعدة الحقول الثلاثة
    │   ├── dish.ts · menu.ts · restaurant.ts
    │   └── subscription.ts · loyalty.ts · support.ts
    │
    ├── lib/
    │   ├── supabase/          ← client.ts · server.ts · admin.ts
    │   ├── db/                ← كل استدعاء Supabase هنا حصراً
    │   │   ├── dishes.ts · menus.ts · restaurants.ts
    │   │   └── subscriptions.ts · analytics.ts · loyalty.ts
    │   ├── paylink/           ← سيرفر فقط، لا يُستورد في مكوّن عميل أبداً
    │   │   ├── client.ts · recurring.ts · webhook.ts
    │   └── utils/             ← phone.ts (تطبيع 966) · whatsapp.ts · image.ts
    │
    ├── components/
    │   ├── ui/                ← أساسيات مشتركة
    │   ├── menu/              ← راشد
    │   └── dashboard/
    │
    └── app/
        ├── layout.tsx         ← dir="rtl" lang="ar" + الخطوط العربية
        ├── page.tsx           ← الصفحة التعريفية
        ├── (auth)/
        │   ├── login/ · register/
        ├── m/[slug]/          ← راشد — المنيو العام (Server Component)
        │
        ├── dashboard/
        │   ├── layout.tsx · page.tsx
        │   ├── menu-editor/   ← نورة
        │   ├── settings/      ← سعد
        │   ├── billing/       ← خالد
        │   ├── reviews/       ← سلمى
        │   ├── loyalty/       ← عمر
        │   ├── ai/            ← ماجد
        │   └── support/       ← ريم
        │
        ├── founder/
        │   ├── page.tsx       ← فهد
        │   ├── restaurants/   ← لمياء
        │   ├── revenue/       ← طارق
        │   ├── content/       ← هند
        │   └── team/          ← يوسف
        │
        └── api/
            ├── paylink/register/route.ts
            ├── paylink/webhook/route.ts
            └── founder/[…]/route.ts    ← بديل بروكسي C5
```

كل ملف رئيسي يبدأ بتعليق يذكر اسمه العربي من خريطة الوكلاء. مثال:

```ts
// نورة — محرر المنيو
```

---

## 3. الحل المعماري لقاعدة الحقول الثلاثة (القسم 2.3)

هذه أخطر بق في المشروع القديم حسب الـ Brief. **لن نعالجها بالانضباط، بل بالمعمارية:**
لا يمكن أن ننسى قائمة إذا لم تكن هناك قوائم أصلاً.

```ts
// src/schemas/dish.ts
import { z } from 'zod';

/** كل حقول الطبق — تُعرَّف مرة واحدة، هنا فقط */
export const dishSchema = z.object({
  id:            z.string().uuid(),
  menu_id:       z.string().uuid(),
  restaurant_id: z.string().uuid(),
  name:          z.string().min(1, 'اسم الطبق مطلوب').max(80),
  description:   z.string().max(300).nullable().default(null),
  price:         z.number().nonnegative('السعر لا يكون سالباً'),
  category:      z.string().max(60).nullable().default(null),
  emoji:         z.string().max(8).default('🍽'),
  image:         z.string().url().nullable().default(null),
  calories:      z.number().int().nonnegative().nullable().default(null),
  featured:      z.boolean().default(false),
  available:     z.boolean().default(true),
  options:       dishOptionsSchema.default([]),
  created_at:    z.string().datetime(),
});

// الثلاثة تُشتقّ تلقائياً — لا قوائم يدوية
export const dishInsertSchema = dishSchema.omit({ id: true, created_at: true });
export const dishUpdateSchema = dishInsertSchema.partial();
export const dishDefaults     = dishInsertSchema.parse({ /* الحد الأدنى */ });

export type Dish       = z.infer<typeof dishSchema>;
export type DishInsert = z.infer<typeof dishInsertSchema>;
```

وطبقة `src/lib/db/` تمرّر **كل** جسم عبر `schema.parse()` قبل إرساله:

```ts
// src/lib/db/dishes.ts
export async function createDish(input: unknown) {
  const data = dishInsertSchema.parse(input);   // يرمي خطأ صريح، لا حذف صامت
  const { data: row, error } = await supabase.from('dishes').insert(data).select().single();
  if (error) throw new DbError('فشل حفظ الطبق', error);
  return dishSchema.parse(row);
}
```

**النتيجة:** إضافة حقل جديد = سطر واحد في الـ schema. يظهر في التهيئة والإدراج والتحديث
في اللحظة نفسها. والحقل المجهول **يُرفض بخطأ صريح** بدل أن يُحذف بصمت.

---

## 4. الثيمات التسعة

بنية الرموز مأخوذة من القديم (`bg`, `bgImage`, `card`, `accent`, `text`, `sub`, `btnTxt`,
`titleFont`, `font`, `radius`, `cardBorder`, `cardShadow`)، والقيَم تُصمَّم من الصفر.
كل ثيم = مجموعة متغيرات CSS. البنية واحدة، والاختلاف في الرموز فقط.

**المقترح — لموافقتك عند المرحلة 4:**

| # | الثيم | العالَم البصري |
|---|---|---|
| 1 | نجدي | بيت طيني — آجُرّي عميق، نقوش هندسية، مساحات صلبة |
| 2 | بحري جدّاوي | روشان وبحر — فيروزي، خشب ساج، تفاصيل مخرّمة |
| 3 | قهوة مختصة | إسمنتي، برتقالي محمّص، شبكة صارمة، جروتيسك عربي |
| 4 | ليل الشاورما | أسود دخاني + نيون، كوفي عريض، طاقة عالية |
| 5 | هندي | زمردي وكركمي، ذهبي عتيق، زخرفة كثيفة |
| 6 | ياباني | أبيض واشي، أحمر شنجو واحد، فراغ واسع |
| 7 | دايْنر أمريكي | أحمر/كريمي، خطوط عريضة جداً، حدود سميكة |
| 8 | حلويات | باستيل، وردي فاتح، حواف مدوّرة كبيرة |
| 9 | عشاء فاخر | أسود + ذهبي **مطفي** لا لامع، تباعد سخي |

**قيود التصميم (القسم 7):**
- تجنّب المظهر الافتراضي المعروف: خلفية كريمية + serif عالي التباين + طيني دافئ.
  لا واحد من التسعة يقع في هذا النمط.
- لكل ثيم **زوج خطوط عربي مقصود** (عرض + متن) — لا خط واحد للجميع.
- الحركة بأقل قدر، مع احترام `prefers-reduced-motion`.

---

## 5. ترتيب البناء والبوابات

### المرحلة 1 — الأساس

- Next.js (أحدث مستقر، App Router) على `main` مباشرة
- `tsconfig.json` بـ `strict: true` · Tailwind · ESLint · Prettier
- **`src/config/*` و `src/schemas/*` قبل أي واجهة** — هذا شرط الـ Brief
- التخطيط RTL الأساسي والخطوط العربية
- نظام الثيمات: البنية فقط، بلا محتوى

**شرط الانتقال:** `npm run build` بصفر أخطاء TypeScript · `npm run lint` بصفر تحذيرات.

### المرحلة 2 — قاعدة البيانات

أربعة ملفات SQL مرقّمة، قابلة للتنفيذ بالترتيب:

| الملف | المحتوى |
|---|---|
| `001_schema.sql` | الجداول والفهارس والدوال — منها `increment_menu_view()` لإصلاح **B1** |
| `002_rls.sql` | **RLS على كل جدول بلا استثناء** + شرح عربي لكل سياسة |
| `003_storage.sql` | bucket `menu-images` وسياسات الوصول |
| `004_seed.sql` | بيانات أولية (مفاتيح الميزات، محتوى الفوتر) |

**سياسات RLS الحاكمة — تعالج ثغرات التدقيق مباشرة:**

| الجدول | السياسة | يعالج |
|---|---|---|
| `subscriptions` | **قراءة فقط للمالك. لا `INSERT` ولا `UPDATE` لأي دور عميل** — الكتابة حصراً بـ service role من webhook أو التفعيل اليدوي | **C2** |
| `loyalty_customers` | لا وصول من دور anon إطلاقاً. كل العمليات عبر Route Handler | **C7** |
| `restaurants` | المالك يقرأ ويكتب صفّه. المنيو العام يقرأ **أعمدة محددة فقط** عبر view | **C4** |
| `analytics` | لا `INSERT` مباشر. الزيادة حصراً عبر `increment_menu_view()` بـ `security definer` | **B1** |
| `dishes` / `menus` | المالك فقط للكتابة، مع **trigger يفرض حدود الباقة** | **B2** |
| `revenue_log` | لا كتابة من العميل إطلاقاً | **B5** |

**دور المؤسس:** عمود `role` في جدول ملفات المستخدمين + دالة `is_founder()` تُستخدم في
السياسات — لا مقارنة إيميل في العميل (**C1**)، ولا بروكسي عام (**C5**).

> # 🛑 STOP GATE 2
> أعرض ملفات SQL مع شرح عربي لكل سياسة. **تنفّذها بنفسك** في Supabase SQL Editor
> وتعطيني مفاتيح البيئة. لن أنشئ مشروع Supabase ولا أنفّذ SQL بنفسي.

بعد التنفيذ: توليد أنواع TypeScript من المخطط عبر `supabase gen types`.

### المرحلة 3 — لوحة المطعم

واحداً واحداً، لا بالتوازي. الترتيب مبني على التبعية:

| # | الوكيل | المجال | التبعية |
|---|---|---|---|
| 1 | **سعد** | الإعدادات العامة | لا شيء — ينشئ المطعم أولاً |
| 2 | **نورة** | محرر المنيو | يحتاج سعد |
| 3 | **عمر** | الولاء | يحتاج نورة |
| 4 | **سلمى** | التقييمات | يحتاج نورة |
| 5 | **ريم** | الدعم | مستقل |
| 6 | **ماجد** | الذكاء الاصطناعي | يحتاج نورة + حد الباقة |

**شرط الانتقال:** كل صفحة تُنشئ وتُعدّل وتحفظ **فعلياً** في Supabase. لا بيانات وهمية.

### المرحلة 4 — المنيو العام (راشد)

أهم صفحة في المنتج. يفتحها زبون من QR على جوال، غالباً على شبكة بطيئة.

- Server Components، صور محسّنة، أقل JS ممكن
- الموبايل أولاً ثم التوسيع
- RTL أصلي — تصميم عربي، لا انعكاس واجهة إنجليزية
- الثيمات التسعة
- **السلة تنتهي برسالة واتساب أو استدعاء طلب — لا شيء آخر** (القسم 6.1)

### المرحلة 5 — الدفع (بيلينك)

| الخطوة | التفصيل |
|---|---|
| 1 | المصادقة → bearer token (سيرفر فقط) |
| 2 | `registerPayment` مع `recurringModel` → يُرجع `recurringId` فيُخزَّن |
| 3 | `getInvoice` → استخراج رابط الدفع |
| 4 | تحويل المطعم لصفحة الدفع |
| 5 | `callBackUrl` — عودة المتصفح فقط، **لا تفعيل** |
| 6 | **Webhook — المصدر الوحيد للحقيقة لتفعيل الاشتراك** |

بيئة تجريبية `restpilot.paylink.sa` ثم إنتاجية `restapi.paylink.sa`.

**القواعد غير القابلة للتفاوض (القسم 6.3):**
`apiId`/`secretKey` على السيرفر فقط · كل استدعاء عبر Route Handler · webhook مُتحقَّق منه ·
معالجة **idempotent** · تسجيل كل حدث في `payment_events` **قبل** معالجته · `.env.local`
في `.gitignore` و `.env.example` بلا قيَم.

> # 🛑 STOP GATE 3
> مخطط التدفق الكامل قبل كتابة أي كود دفع.

### المرحلة 6 — لوحة المؤسس

فهد (نظرة عامة) · لمياء (المطاعم) · **طارق (الإيرادات + التفعيل اليدوي)** · هند (المحتوى) ·
يوسف (الفريق).

**التفعيل اليدوي (القسم 6.5) ليس ميزة ثانوية — هو طريق الإيراد الأول.** يُبنى كاملاً:
زر يفعّل مطعماً لعدد أشهر محدد دون بوابة دفع، بصلاحية سيرفر، مع تسجيل في `revenue_log`.

لوحة الإيرادات تعرض **الصافي** لا الإجمالي، بحساب رسوم بيلينك من `src/config/pricing.ts`.

### المرحلة 7 — التشديد والنشر

اختبارات · معالجة الأخطاء · الحالات الفارغة · حالات التحميل · الاستجابة للموبايل ·
النشر على Netlify.

---

## 6. التطويرات الخمسة المطلوبة (القسم 8)

| # | التطوير | المرحلة | ملاحظة من التدقيق |
|---|---|---|---|
| 1 | رفع الصور إلى Storage | 3 | **مبني في القديم ويعمل** — الناقص الأحجام المتعددة فقط |
| 2 | خيارات الأطباق المركّبة | 3 | مبنية جزئياً — تُعاد على schema بـ zod |
| 3 | تتبّع المشاهدات | 2 + 3 | **إصلاح B1** — دالة `increment_menu_view()` بدل upsert |
| 4 | تفعيل يدوي للاشتراك | 6 | نموذج القديم صالح، يُعاد بصلاحية سيرفر |
| 5 | تطبيق حدود الخطط فعلياً | 2 + 3 | **إصلاح B2** — في `lib/db/` **و** في trigger معاً |

**المقترحة — لا تُبنى إلا بموافقة صريحة:**
تصدير QR بجودة طباعة (6) · تبديل توفّر الطبق فوراً (7) · فترة تجريبية 14 يوماً (8) ·
حالة فارغة ذكية (9).

**لا يُبنى إطلاقاً:** تطبيق جوال · لوحات إحصائية معقّدة · أي ميزة AI جديدة · دعم عدة فروع.

---

## 7. تقدير المراحل

| المرحلة | الحجم | البوابة |
|---|---|---|
| 1 — الأساس | صغيرة | build + lint بصفر أخطاء |
| 2 — قاعدة البيانات | متوسطة | 🛑 GATE 2 |
| 3 — لوحة المطعم | **الأكبر** | كل صفحة تحفظ فعلياً |
| 4 — المنيو العام | كبيرة (التصميم) | 375px + سرعة |
| 5 — الدفع | متوسطة | 🛑 GATE 3 |
| 6 — لوحة المؤسس | متوسطة | التفعيل اليدوي يعمل |
| 7 — التشديد | متوسطة | القسم 10 كاملاً |

المرحلتان 3 و 4 هما ثقل المشروع. المرحلة 2 هي أخطرها — كل ثغرات التدقيق تُغلق فيها أو
تبقى مفتوحة للأبد.

---

## 8. معايير القبول (القسم 10)

لا تنتهي أي مرحلة حتى:

- [ ] `npm run build` ينجح — صفر أخطاء TypeScript
- [ ] `npm run lint` ينجح — صفر تحذيرات
- [ ] لا ملف يخرق قواعد التسمية في القسم 2.1
- [ ] لا رقم سعر أو حد خطة مكتوب حرفياً خارج `src/config/`
- [ ] كل جدول جديد له سياسة RLS
- [ ] كل حقل جديد يمر عبر schema واحد
- [ ] لا مفتاح أو سر في كود العميل
- [ ] `PROGRESS.md` محدّث
- [ ] كل صفحة جديدة تعمل على عرض 375px

---

## 9. الخطوة التالية

> # 🛑 STOP GATE 1
> هذا الملف و `AUDIT.md` معروضان الآن. **لا كود حتى تُوافَق الخطة صريحاً.**

عند الموافقة نبدأ **المرحلة 1 — الأساس**.
