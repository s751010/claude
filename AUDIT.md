# AUDIT.md — تدقيق المشروع القديم

> **المرحلة 0 من `CLOUDMENU_MASTER_BRIEF.md`** — تحليل بلا كود.
> تاريخ التدقيق: 2026-07-27 · المدقِّق: جلسة إعادة البناء.

---

## 0. نطاق التدقيق وما وجدته فعلاً

الـ Brief (القسم 1) يفترض وجود مسارين متوازيين: ملف HTML قديم، ومشروع Next.js على فرع
`claude/awesome-*`، و `main` شبه فارغ.

**الواقع مختلف — وأبسط:**

| ما توقّعه الـ Brief | ما وجدته |
|---|---|
| مستودع فيه فروع متعددة | مستودع `s751010/claude` **فارغ تماماً** — صفر commits، صفر فروع على `origin` |
| فرع `claude/awesome-*` فيه Next.js | **غير موجود** |
| `main` شبه فارغ | **`main` غير موجود أصلاً** |
| ملف HTML مُصغّر ~1.4MB | ✅ موجود — `index_19.html` (1,486,607 بايت، 1947 سطر) |

**الخلاصة:** لا يوجد ما نهاجره من git، ولا فرعان متعارضان، ولا تكرار بين نسختين.
المشكلة الموصوفة في القسم 1 حُلّت بالأمر الواقع. نبدأ من شجرة نظيفة تماماً.

**مصدر التدقيق الوحيد:** البندل المرفوع `index_19.html` — تطبيق React مُصغّر (bundled)،
منشور على `https://cloudsmenu.netlify.app`، متصل بمشروع Supabase `wjqpsbpebpntpeinqccl`.

---

## 1. 🚨 تنبيه عاجل — الموقع القديم منشور الآن وفيه ثغرتان مستغَلّتان

هذا لا يحتمل الانتظار حتى تنتهي إعادة البناء. الموقع حيّ الآن، والثغرتان C1 و C2 أدناه
قابلتان للاستغلال من أي زائر دون أدوات خاصة.

**إجراءات فورية موصى بها، بهذا الترتيب:**

| # | الإجراء | السبب |
|---|---|---|
| 1 | أنزِل `cloudsmenu.netlify.app` أو ضَع عليه Basic Auth | يوقف C1 و C2 فوراً |
| 2 | أبطِل مفتاح ميسر المنشور وأعِد توليده من لوحة ميسر | C6 — مفتاح عام في كود منشور |
| 3 | غيّر كلمة مرور حساب المؤسس، وأبطِل `x-founder-secret` الحالي | C1 و C5 |
| 4 | راجع جدول `subscriptions` في المشروع القديم | C2 — قد تكون هناك اشتراكات ممنوحة ذاتياً |
| 5 | راجع سياسات RLS على `loyalty_customers` | C7 — أرقام جوالات زبائن |

---

## 2. 🔴 حرِج — لا يُنقل إلى المشروع الجديد أبداً

### C1 — تجاوز صلاحية المؤسس في كود العميل

الكود يفتح لوحة المؤسس بمجرد مطابقة نصّية للإيميل، **دون أي تحقق من كلمة المرور**:

```js
if (email === '<إيميل المؤسس مكشوف في البندل>') {
  sessionStorage.setItem('mh_admin_sess', 'admin-1');
  sessionStorage.setItem('cm_fsecret', password);  // ← كلمة المرور المكتوبة تُخزَّن كسر
  setIsAdmin(true);
  return { ok: true, isAdmin: true };
}
```

| الجانب | التفصيل |
|---|---|
| **الأثر** | أي زائر يكتب هذا الإيميل مع **أي** كلمة مرور يفتح واجهة لوحة المؤسس كاملة |
| **التفاقم** | الإيميل نفسه (بريد Gmail شخصي حقيقي) مكشوف حرفياً في البندل المنشور |
| **الحدّ الوحيد** | العمليات الفعلية تمر عبر `founder-admin` التي تتحقق من السر — فالكتابة محدودة، لكن **بنية الواجهة والبيانات المعروضة مكشوفة** |
| **في الجديد** | صلاحية المؤسس تُحسم **على السيرفر** عبر عمود دور في قاعدة البيانات + RLS، لا بمقارنة نصّية في العميل |

### C2 — تفعيل الاشتراك من العميل مباشرة (تسريب إيراد كامل)

```js
const [sub] = await rest('subscriptions', {
  method: 'POST',
  body: JSON.stringify({
    user_id: user.id,
    plan_id: planId,
    payment_ref: ref,        // ← يحدّده العميل
    end_date: endDate,       // ← يحدّده العميل
    active: true             // ← يحدّده العميل
  })
}, userToken);
```

| الجانب | التفصيل |
|---|---|
| **الأثر** | أي مستخدم مسجَّل يمنح نفسه أعلى باقة، بأي تاريخ انتهاء، بطلب واحد من devtools |
| **السبب الجذري** | الاشتراك يُكتب بتوكن المستخدم نفسه، ولا يوجد تحقق من دفع فعلي في أي طبقة |
| **في الجديد** | **لا يُكتب صف اشتراك إلا من webhook بيلينك مُتحقَّق منه** (القسم 6.3 بند 3)، أو من مسار التفعيل اليدوي بصلاحية service role. RLS تمنع `INSERT` و `UPDATE` على `subscriptions` من أي دور عميل |

### C3 — بوابة دفع داخل المنيو (ميسر) — مخالفة قانونية

```js
{ label: 'الدفع الإلكتروني (Moyasar)',
  desc: 'بوابة دفع أونلاين داخل المنيو',
  k: 'payment_enabled', disabled: !feat.orders_enabled }
```

مفتاح ميزة `payment_enabled` في `site_settings` يفعّل استلام دفعات زبائن المطعم داخل المنيو.

**هذا هو بالضبط النموذج الممنوع في القسم 6.1** — نموذج الحسابات التجميعية الذي يحتاج
ترخيص البنك المركزي السعودي (ساما) أو خطاب عدم ممانعة.

**القرار:** يُحذف بالكامل — المفتاح، والواجهة، والأعمدة المرتبطة به، ومكتبة ميسر.
المسار الوحيد المسموح للسلة هو رسالة واتساب أو استدعاء طلب (وهو مبني فعلاً — انظر القسم 5).

### C4 — مفتاح بوابة دفع لكل مطعم داخل جدول `restaurants`

```js
{ payment_gateway: r.payment_gateway || 'none',
  payment_key:     r.payment_key     || '' }
```

عمودان يُقرآن ويُكتبان من العميل. سرّ بوابة دفع مخزَّن في صف تقرأه الواجهة.
يُحذف مع C3. **القسم 6.3 بند 1: أي مفتاح في كود العميل = فشل فوري.**

### C5 — `founder-admin` بروكسي مفتوح لأي جدول

```js
fetch(`${SUPABASE_URL}/functions/v1/founder-admin`, {
  method: 'POST',
  headers: { apikey: ANON, 'x-founder-secret': sessionStorage.getItem('cm_fsecret') },
  body: JSON.stringify({ table, method, query, body })   // ← الجدول والعملية من العميل
});
```

| الجانب | التفصيل |
|---|---|
| **الأثر** | البوابة الوحيدة سرّ ثابت مشترك. من يحصل عليه يقرأ ويكتب على **أي** جدول بأي عملية |
| **التفاقم** | السر هو كلمة مرور المؤسف المكتوبة في نموذج الدخول، مخزَّنة في `sessionStorage` — عرضة لأي XSS |
| **في الجديد** | لا بروكسي عام. كل عملية مؤسس لها Route Handler خاص بمدخلات مُتحقَّق منها بـ zod، والصلاحية من جلسة Supabase + دور في قاعدة البيانات |

### C6 — مفتاح ميسر منشور في البندل

| ما وجدته | الحالة |
|---|---|
| `pk_test_ZcUaLJxdz27Fc9SkQyneSfuq193EBUmMr8tBqHj7` | مفتاح تجريبي حقيقي، منشور حرفياً |
| `pk_live_xxxxxxxxxxxxxxxx` | placeholder فقط — لم يُملأ |

يطابق التنبيه في القسم 9 من الـ Brief. مفاتيح `pk_` عند ميسر قابلة للنشر بحكم التصميم،
لكن **القرار: يُبطَل ويُعاد توليده** — لأن الحساب سيُهجر لصالح بيلينك على أي حال، ولأن
بقاء مفتاح حيّ في كود مهجور مخاطرة بلا مقابل.

**لا يُنقل أي مفتاح من المشروع القديم إلى الجديد.**

### C7 — `loyalty_customers` تُقرأ وتُكتب من المنيو العام بمفتاح anon

```js
// قراءة — من صفحة المنيو العامة، بمفتاح anon
fetch(`${URL}/rest/v1/loyalty_customers?restaurant_id=eq.${rid}&phone=eq.${phone}&select=*`)

// تعديل الأختام — من نفس الصفحة
fetch(`${URL}/rest/v1/loyalty_customers?id=eq.${found.id}`, { method: 'PATCH', ... })
```

| الجانب | التفصيل |
|---|---|
| **الأثر (إن كانت RLS مرتخية)** | `select=*` بمفتاح anon يكشف **أرقام جوالات زبائن المطعم** — بيانات شخصية |
| **الأثر الثاني** | `PATCH ?id=eq.X` بمفتاح anon يعني أن أي زائر يعدّل أختام أي زبون |
| **لماذا "إن كانت مرتخية"** | لم أستطع فحص سياسات RLS الفعلية على المشروع القديم من البندل وحده. الكود **يفترض** أنها تسمح، وإلا لما عملت الميزة |
| **في الجديد** | الأختام تُعدَّل عبر Route Handler بصلاحية سيرفر بعد تحقق، ولا يُقرأ رقم جوال في المنيو العام إطلاقاً |

> ### ملاحظة عن مفتاح Supabase الـ anon المكشوف
> البندل يحوي `SUPABASE_URL` و anon key للمشروع `wjqpsbpebpntpeinqccl` بشكل صريح.
> **هذا ليس ثغرة بحد ذاته** — مفاتيح anon مصمَّمة لتكون عامة، وهي كذلك في كل تطبيق Supabase.
> الخطر كله في ما خلفها: C1 و C2 و C7. أذكره هنا للتوثيق لا للإنذار.

---

## 3. 🟠 بقّ مؤكّد

### B1 — عدّاد المشاهدات لا يتراكم في قاعدة البيانات

```js
fetch(`${URL}/rest/v1/analytics`, {
  method: 'POST',
  headers: { Prefer: 'resolution=merge-duplicates,return=minimal' },
  body: JSON.stringify({ menu_id, date, hour, views: 1 })   // ← ثابت 1، دائماً
});
```

| الجانب | التفصيل |
|---|---|
| **المشكلة** | `resolution=merge-duplicates` في PostgREST تترجم إلى `ON CONFLICT DO UPDATE SET views = 1` — أي **تُعيد القيمة إلى 1**، لا تزيدها |
| **لماذا لم يُكتشف** | الواجهة تزيد العدّاد **محلياً** في حالة React: `views: (t.views \|\| 0) + 1` — فيبدو أنه يعمل حتى أول تحديث للصفحة، ثم يعود إلى 1 |
| **الأثر** | كل بيانات المشاهدات في المشروع القديم **بلا قيمة** — الحد الأقصى لأي خانة (منيو، يوم، ساعة) هو 1 |
| **الإصلاح في الجديد** | دالة Postgres `increment_menu_view(menu_id)` تُستدعى بـ RPC وتنفّذ `ON CONFLICT DO UPDATE SET views = analytics.views + 1`. يطابق التطوير رقم 3 في القسم 8 |

### B2 — حدود الخطط معرَّفة ولا تُطبَّق إطلاقاً

```js
const PLAN_LIMITS = {
  basic:    { menus: 1,   dishes: 40,  ai: false },
  standard: { menus: 3,   dishes: 100, ai: true  },
  premium:  { menus: 999, dishes: 999, ai: true  }
};
```

| الجانب | التفصيل |
|---|---|
| **المشكلة** | `planLimits` يُمرَّر عبر الـ context ويُفكَّك في **3 مكوّنات**، ولا يُقارَن بأي عدّاد في **أي** موضع في البندل كله |
| **التحقق** | بحثت عن كل استخدام لـ `planLimits.dishes` و `planLimits.menus` و `planLimits.ai` — **صفر نتائج**. المتغيّر يُستخرج ثم يُهمَل |
| **الأثر** | مشترك الباقة الأرخص يضيف أطباقاً ومنيوهات بلا حد. الفرق بين الباقات **تسويقي فقط** |
| **الإصلاح في الجديد** | يطابق التطوير رقم 5 في القسم 8. يُطبَّق في **طبقتين**: `src/lib/db/` قبل الإدراج (لرسالة خطأ عربية واضحة)، و RLS/trigger في قاعدة البيانات (لأن الواجهة وحدها ليست حاجزاً) |

### B3 — عدد الثيمات مكتوب بثلاث قيم متناقضة

| الموضع | القيمة المعلَنة |
|---|---|
| صفحة التسويق | `5 ثيمات قابلة للتخصيص` |
| بطاقة السعر (باقة أساسي) | `كل الثيمات (12 ثيم)` |
| خريطة الرموز الفعلية في الكود | **8** ثيمات |
| ما يطلبه الـ Brief (القسم 7) | **9** ثيمات |

الثيمات الثمانية الموجودة فعلاً: `dark-gold` · `lux` · `youth` · `heritage` · `minimal` ·
`noir` · `royal` · `midnight`.

**مثال حيّ ومباشر على غياب مصدر الحقيقة الواحد** — وهو ما يعالجه القسم 2.2 بـ `src/config/`.

### B4 — مصفوفة الأسعار مكرَّرة حرفياً في مكوّنين

نفس المصفوفة، نسختان منفصلتان، بلا استيراد مشترك:

```js
[ { id: 'basic',    name: 'أساسي',  price: 49,  yearly: 559  },
  { id: 'standard', name: 'نمو',    price: 99,  yearly: 1129 },
  { id: 'premium',  name: 'احتراف', price: 199, yearly: 2269 } ]
```

القيَم متطابقة **اليوم** — لكن هذه بالضبط البنية التي أنتجت المشكلة الموصوفة في القسم 2.2
(سعر واحد في ثلاثة أماكن بثلاث قيم). تعديل واحدة دون الأخرى مسألة وقت.

### B5 — الخصم يُحسب في العميل ثم يُكتب في سجل الإيراد

```js
const amount = promo ? Math.round(base * (1 - promo.discount / 100)) : base;
await rest('revenue_log', { method: 'POST', body: JSON.stringify({ amount, ... }) });
```

مبلغ الإيراد المخزَّن يحدّده العميل. أرقام لوحة الإيرادات غير موثوقة بنيوياً.

### B6 — الثيم المولَّد بالـ AI يُخزَّن كـ JSON داخل عمود `theme` النصي

```js
const theme = (menu.theme && menu.theme.indexOf('ai:') === 0)
  ? JSON.parse(menu.theme.slice(3))
  : THEMES[menu.theme] || THEMES['dark-gold'];
```

عمود واحد يحمل معنيين: معرّف ثيم، أو JSON كامل مسبوق بـ `ai:`. لا فهرسة، ولا تحقق،
و `JSON.parse` داخل `try/catch` يبتلع الخطأ ويعيد `null` بصمت.

### B7 — الجلسة JWT يدوية في `localStorage`

```js
localStorage.setItem('cm_session', JSON.stringify({ access_token, user, expires_at }));
```

بدل `@supabase/ssr`. النتائج: لا تجديد تلقائي للتوكن (المستخدم يُطرد عند الانتهاء)،
ومساحة هجوم XSS، ولا تكامل مع Server Components.

### B8 — `promo_codes` بمصدرين للحقيقة

الأكواد تُقرأ أحياناً من جدول `promo_codes`، وأحياناً من `localStorage` عبر مفتاح
`PROMO_CODES`. مساران للبيانات نفسها.

### B9 — `catch` فارغ في مسارات الشبكة

```js
}).catch(() => {})              // تسجيل المشاهدة
} catch (e) { }                 // مسارات أخرى
```

يخالف القسم 5: «كل استدعاء يمكن أن يفشل يجب أن يعالج الفشل. لا `catch` فارغ.»
الأثر العملي: فشل تسجيل المشاهدة أو الحفظ لا يظهر للمستخدم ولا يُسجَّل.

---

## 4. 📊 جرد قاعدة البيانات

**13 جدولاً** (الـ Brief ذكر 11 — الفرق على الأرجح جدولان أُضيفا بعد كتابته):

| الجدول | الاستخدام في البندل |
|---|---|
| `restaurants` | بيانات المتجر، الثيم، ساعات العمل، الحساسية، السوشال، **و `payment_gateway`/`payment_key`** (يُحذفان) |
| `menus` | المنيوهات، `theme`، `views`، `active` |
| `dishes` | الأطباق، `price`، `category`، `emoji`، `featured`، `image`، `calories`، `options` |
| `subscriptions` | **يُكتب من العميل — C2** |
| `analytics` | `menu_id`/`date`/`hour`/`views` — **B1** |
| `revenue_log` | **المبلغ يحدّده العميل — B5** |
| `loyalty_customers` | `phone`، `card_code`، `stamps` — **C7** |
| `survey_responses` | إجابات الاستبيان، تُكتب من المنيو بمفتاح anon |
| `support_tickets` | التذاكر + `admin_reply` |
| `promo_codes` | `code`، `discount`، `max_uses`، `expiry_date`، `uses` — **B8** |
| `announcements` | إعلانات (`info`/`warning`/`offer`) |
| `blog_posts` | المدونة |
| `site_settings` | `key`/`value` — يحمل مفاتيح الميزات `orders_enabled` و **`payment_enabled` (C3)** |

**Storage:** bucket واحد `menu-images`، المسار `${user_id}/${timestamp}-${rand}.${ext}`.

**Edge Functions:** `ai-proxy` (وسيط OpenAI — يقرأ `OPENAI_API_KEY` من Supabase Secrets) ·
`founder-admin` (**C5**).

---

## 5. 🟢 جاهز وقابل لإعادة الاستخدام

| المكوّن | الحالة الفعلية | ما يُنقل وما يُعاد |
|---|---|---|
| **بنية رموز الثيمات** | الهيكل ممتاز: `bg, bgImage, card, accent, text, sub, btnTxt, titleFont, font, radius, cardBorder, cardShadow` | **الهيكل يُنقل، والقيَم تُصمَّم من الصفر** (قرار الجلسة) |
| **رفع الصور + الضغط** | **مبني ويعمل** — خلافاً لما يفترضه القسم 8 («كان معلّقاً») | canvas يوازن WebP مقابل JPEG ويختار الأصغر حجماً، ثم يرفع إلى `menu-images`. **الناقص: الأحجام المتعددة فقط** |
| **خيارات الأطباق** | مبنية جزئياً — `parseOpts(dish.options)` + `cartMeta[id].price` لتعديل السعر | المنطق سليم، لكن التخزين نصّي بلا تحقق. **يُعاد على schema بـ zod** |
| **توليد QR** | مبني — مكتبة `qrcode` مضمّنة، `toDataURL(url, { width: 600 })` | يُنقل. **الناقص: تصدير بجودة طباعة** (البند 6 المقترح) |
| **سلة → واتساب** | **مبنية وصحيحة قانونياً** | تبني رسالة منسّقة بالأصناف والكميات والإجمالي، وتطبّع رقم الجوال السعودي إلى `966…` (تتعامل مع `00` و `05`). **هذا هو المسار الوحيد المسموح في القسم 6.1** — يُنقل كما هو |
| **التفعيل اليدوي للاشتراك** | مبني في لوحة المؤسس | `payment_ref: 'ADMIN-' + code` و `action: 'admin_extend'`. نموذج جيد للبند 6.5 — **يُعاد بناؤه بصلاحية سيرفر** |
| **Edge Function `ai-proxy`** | قابل للنقل (القسم 9) | يُراجع: يجب ألا يُستدعى بمفتاح anon بلا تحقق من الباقة |
| **الفوتر / الاستبيان / التذاكر / المحتوى** | نصوص وبنية قابلة للنقل | تُنقل إلى `src/config/strings.ts` |

---

## 6. 🗑️ مقترح للحذف — بحزم

| العنصر | السبب |
|---|---|
| **كل مسار الدفع داخل المنيو** (`payment_enabled`، `payment_gateway`، `payment_key`، ميسر) | قيد قانوني — القسم 6.1. غير قابل للنقاش |
| **باقة "أساسي" 49/559** | قرار هذه الجلسة — نبقي باقتين فقط |
| **فريق الاستشارة بالـ AI** (`ceo`/`cmo`/`cto`/`cfo`/`cs`/`growth`) | غير مذكور في خريطة الوكلاء، لا علاقة له بالمنتج، **وأسماؤه تتضارب مع خريطة الوكلاء** — انظر القسم 7 |
| **`recharts`** (78 إشارة في البندل) | القسم 5: لا حزمة رسوم بيانية قبل الحاجة الفعلية. مساهم رئيسي في حجم الـ 1.4MB |
| **`redux`** (15 إشارة) | القسم 5: لا مكتبة حالة عامة |
| **مسار `promo_codes` في `localStorage`** | مصدر حقيقة ثانٍ — B8 |
| **عمود `theme` الحامل لـ `ai:{json}`** | يُستبدل بـ `theme_id` نصّي مُقيَّد + عمود `custom_theme` من نوع `jsonb` |
| **الجلسة اليدوية في `localStorage`** | تُستبدل بـ `@supabase/ssr` — B7 |
| **`founder-admin` كبروكسي عام** | يُستبدل بـ Route Handlers محددة — C5 |

---

## 7. ⚠️ تضارب أسماء يجب حسمه الآن

أسماء خريطة الوكلاء في الـ Brief مستخدَمة في الكود القديم لشخصيات فريق AI **بأدوار مختلفة تماماً**:

| الاسم | دوره في خريطة الوكلاء (الـ Brief) | دوره في الكود القديم (فريق AI) |
|---|---|---|
| **نورة** | محرر المنيو | مديرة التسويق |
| **خالد** | الدفع | مدير نجاح العملاء |
| **ريم** | الدعم | مديرة المالية |
| **سلمى** | التقييمات | محللة النمو |

قول «عدّل عند نورة» سيكون غامضاً ما دامت الميزتان موجودتين معاً.

**القرار:** حذف ميزة فريق الـ AI يحلّ التضارب نهائياً — وهذا سبب إضافي حاسم للحذف،
فوق كونها ميزة خارج نطاق المنتج.

---

## 8. ملخص تنفيذي

| الفئة | العدد | الأخطر |
|---|---|---|
| 🔴 حرِج | 7 | C2 — تسريب إيراد كامل · C3 — مخالفة القسم 6.1 |
| 🟠 بقّ مؤكّد | 9 | B1 — كل بيانات المشاهدات بلا قيمة · B2 — الباقات بلا فرق فعلي |
| 🟢 قابل لإعادة الاستخدام | 8 | رفع الصور + الضغط (مبني، خلافاً للمفترض) · سلة واتساب |
| 🗑️ مقترح للحذف | 9 | الدفع داخل المنيو · فريق الـ AI · recharts + redux |

**الحكم العام:** البندل القديم يصلح **مرجعاً** لا **أساساً**. الجزء الأثمن فيه ليس الكود
بل المعرفة: بنية رموز الثيمات، منطق رسالة واتساب، وتطبيع رقم الجوال السعودي.
كل ما عدا ذلك يُعاد بناؤه.

الخطة في `PLAN.md`.
